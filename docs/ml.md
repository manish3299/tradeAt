# Machine Learning Notes

Begin with interpretable baselines and treat model outputs as versioned plugins. Dataset manifests capture feature/label definitions, availability timestamps, universe, window, exclusions, split/embargo, source revisions, and content hash. A model card records intended use, prohibited use, training data, evaluation cohorts, calibration, limitations, latency, monitoring, and rollback.
