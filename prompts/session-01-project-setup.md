# Session 01: Project Setup

Choose and record the backend/frontend stack, then scaffold both applications, local PostgreSQL/Redis dependencies, environment validation, structured logging/tracing, health endpoints, migrations, unit/integration test harnesses, formatting/lint/type/build scripts, and CI. Provide one documented command path for local startup. Do not add feature placeholders. Exit only when milestone 1 criteria and available gates pass.
